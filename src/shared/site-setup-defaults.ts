// The one wp-admin account every LocalWP install Muster creates gets. Local requires an account to
// build the install; it is local-only, never asked for, and shown once on the done screen so a
// user who needs to log in is not left guessing (ocsites used the same house account).
export const LOCALWP_ADMIN_EMAIL = 'hello@efront.com.au'
export const LOCALWP_ADMIN_PASSWORD = 'admin'
