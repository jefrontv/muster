export const AUTO_UPDATE_CHECK_INTERVAL_MS = 60 * 60 * 1000
export const AUTO_UPDATE_RETRY_INTERVAL_MS = 60 * 60 * 1000
/** Wall-clock poll that catches a timer the Mac slept through, or a settle path that never re-armed. */
export const AUTO_UPDATE_HEARTBEAT_MS = 5 * 60 * 1000
