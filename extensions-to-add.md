Extensions/MCPS TO ADD:

- [x] [context7](https://github.com/upstash/context7) — shipped as `context7-mcp` in the catalog.
  ```
  Global npm install of `@upstash/context7-mcp`, stdio, version tracked against the npm tag.
  An API key is optional and still manual: add `CONTEXT7_API_KEY` to the entry Muster writes.
  ```

Add more below and I will work out the install and setup for each.



[https://github.com/rtk-ai/rtk](https://github.com/rtk-ai/rtk)

(add skill from my client-friendly-explaination skill in claude)

**efront-dev-index-audit (from my skills)**